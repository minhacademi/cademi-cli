name: Feature request
description: Suggest something new for the Cademí API v3 or the cademi CLI.
labels: ["enhancement", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Please write in English, so everyone who integrates with Cademí can follow the conversation.
  - type: dropdown
    id: product
    attributes:
      label: For
      options:
        - API
        - CLI
        - Both
    validations:
      required: true
  - type: textarea
    id: problem
    attributes:
      label: What you are trying to do
      description: The task or workflow, and what makes it hard today.
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: What you would like
      description: The endpoint, field, event, command, or flag you have in mind. An example helps.
  - type: input
    id: version
    attributes:
      label: CLI version or API release
      placeholder: cademi 0.1.3 / API 3.4.4
