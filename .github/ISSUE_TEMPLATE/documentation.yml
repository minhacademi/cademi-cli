name: Documentation problem
description: Something in the API or CLI documentation at cademi.dev is wrong, unclear, or missing.
labels: ["documentation", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Please write in English, so everyone who integrates with Cademí can follow the conversation.
  - type: input
    id: page
    attributes:
      label: Page
      description: The URL of the page.
      placeholder: https://cademi.dev/api/webhooks
    validations:
      required: true
  - type: textarea
    id: problem
    attributes:
      label: What is wrong or missing
      description: Quote the text if it helps. If the API or the CLI behaves differently from the page, include the request or command and its output.
    validations:
      required: true
  - type: textarea
    id: suggestion
    attributes:
      label: Suggested change
